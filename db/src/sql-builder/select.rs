use super::*;

impl SqlBuilder {
    // pub fn select(from: String, columns: Option<Vec<String>>) -> Self {
    //     Self {
    //         operation: Operation::Select(columns, from),
    //     }
    // }

    // pub fn where_and(from: String, columns: Option<Vec<String>>) -> Self {
    //     Self {
    //         operation: Operation::Select(columns, from),
    //     }
    // }
}
